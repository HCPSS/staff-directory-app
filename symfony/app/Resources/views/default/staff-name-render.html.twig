{% extends isAjax ? 'default/blank.html.twig' : 'default/staff-search.html.twig' %}

{% block body %}
    {% if isAjax == false %}
    	{{ parent() }}
    {% endif %}
    
    {% if staff != null %}
        {% for person in staff %}
            {{ include('default/card-data.html.twig') }}
        {% endfor %}
    {% elseif phone != null %}
        {% for person in phone %}
            {{ include('default/card-data.html.twig') }}
        {% endfor %}
    {% else %}
        {% for depts in dept %}
            {{ include('default/card-data.html.twig') }}
        {% endfor %}
    {% endif %}
{% endblock %}
